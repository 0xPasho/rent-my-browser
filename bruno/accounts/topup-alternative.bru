meta {
  name: Top Up Alternative (sandbox only)
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/accounts/credits/alternative
  body: json
  auth: bearer
}

auth:bearer {
  token: {{apiKey}}
}

body:json {
  {
    "amount": 1000
  }
}
