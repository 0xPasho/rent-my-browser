meta {
  name: Top Up (Stripe)
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/accounts/credits/stripe
  body: json
  auth: bearer
}

auth:bearer {
  token: {{apiKey}}
}

body:json {
  {
    "amount": 1000
  }
}
