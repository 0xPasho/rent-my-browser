meta {
  name: Confirm Consumer Account
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/accounts/confirm
  body: json
  auth: none
}

body:json {
  {
    "wallet_address": "{{walletAddress}}",
    "tx_hash": "{{txHash}}"
  }
}
