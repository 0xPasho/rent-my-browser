meta {
  name: Confirm Operator Account
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/nodes/confirm
  body: json
  auth: none
}

body:json {
  {
    "wallet_address": "{{walletAddress}}",
    "tx_hash": "{{txHash}}",
    "node_type": "real"
  }
}
